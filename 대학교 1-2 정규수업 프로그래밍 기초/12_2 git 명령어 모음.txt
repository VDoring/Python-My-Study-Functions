pwd = 현재 내 폴더 위치

dir / ll(리눅스명령어) = 파일 등을 볼 수 있음

mkdir = 폴더 만들기

rm -r [파일/폴더 이름] = 파일/폴더 삭제

cp [원본파일장소] [복사해서붙여넣을장소] = 원본파일을 복사해서 붙여넣을장소에 파일을 붙여넣는다.

vim [코드파일이름] = 코드보기 모드로 전환
Shift + ; = vim모드 안에서 입력모드로 전환
;q + Enter = vim모드를 종료함